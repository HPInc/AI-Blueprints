[tool.poetry]
name = "data-analysis-with-var-streamlit"
version = "0.1.0"
description = "Streamlit demo for Data Analysis with VAR blueprint"
authors = ["HP AI Studio <datastudio@hp.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
streamlit = "^1.28.0"
pandas = "^2.0.0"
numpy = "^1.24.0"
matplotlib = "^3.7.0"
seaborn = "^0.12.0"
plotly = "^5.15.0"
statsmodels = "^0.14.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
